# Core web framework - REQUIRED
fastapi==0.111.0
uvicorn[standard]==0.31.1
pydantic>=2.0.0,<2.12
pydantic-settings>=2.0.0

# Environment and configuration - REQUIRED
python-dotenv==1.0.1

# Azure SDK packages - REQUIRED for your app
azure-identity==1.17.0
azure-cosmos>=4.7.0
azure-ai-projects>=1.0.0b1

# HTTP and utilities - REQUIRED
requests>=2.31.0
python-dateutil>=2.8.2

# Optional packages (can be removed for minimal deployment)
# python-multipart>=0.0.7  # Only needed if you upload files
# azure-core>=1.30.0  # Usually comes with other azure packages
# azure-search-documents==11.5.3  # Only if using direct search (you have plugins)
# azure-keyvault-secrets==4.7.0  # Only if using key vault directly
# semantic-kernel==1.35.2  # Only if using SK plugins (you might not need this in container)
# openai>=1.0.0  # Only if making direct OpenAI calls
# pyodbc>=5.1.0  # Only if connecting to SQL Server directly
# httpx>=0.25.2  # Only if making HTTP requests with httpx instead of requests
# cachetools>=5.3.0  # Only if using caching

# Testing packages (remove for production)
# pytest==7.4.3
# pytest-asyncio==0.21.1

# Agent framework packages (causing conflicts - consider removing for deployment)
# agent-framework-core>=1.0.0b251001
# agent-framework>=1.0.0b251001
# agent-framework-azure-ai>=1.0.0b251001