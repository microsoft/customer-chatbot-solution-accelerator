# Core web framework
fastapi==0.119.0
uvicorn[standard]==0.38.0
pydantic[email]==2.12.3
python-dotenv==1.1.1

# Azure SDK Core
azure-core==1.35.1
azure-identity==1.25.1
requests==2.32.5

# Azure Services (only what you actually use)
azure-search-documents==11.6.0
azure-ai-projects==1.0.0
azure-cosmos==4.9.0

# Agent framework (if you're using it in production)
agent-framework==1.0.0b251016

# Optional packages (remove if not needed):
# cachetools==6.2.1  # Only if you're using caching
# azure-ai-inference==1.0.0b9  # Only if making direct inference calls
# openai==1.109.1  # Only if making direct OpenAI calls (you use Azure AI)
# pandas==2.3.3  # Only if doing data processing
# pyodbc==5.2.0  # Only if connecting to SQL Server directly

# Development packages (remove for production):
# pytest==8.4.2
# pytest-cov==7.0.0
# pytest-asyncio==1.2.0